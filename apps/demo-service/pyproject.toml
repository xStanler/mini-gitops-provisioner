[project]
name = "demo-service"
version = "0.1.0"
description = "Simple FastAPI app with few endpoints."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.0",
    "uvicorn>=0.50.2",
]
